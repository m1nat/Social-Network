import s from './photo.module.scss';

const Photo = () => {
    return(
        <div className={s.photo}>
            <p className={s.photoText}>Photo</p>
        </div>
    )
};

export default Photo;