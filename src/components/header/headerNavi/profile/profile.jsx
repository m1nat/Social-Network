import s from './profile.module.scss';

const Profile = () => {
    return(
        <div className={s.profile}>
            <p className={s.profileText}>Profile</p>
        </div>
    )
};

export default Profile;