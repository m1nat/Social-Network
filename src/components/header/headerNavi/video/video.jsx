import s from './video.module.scss';

const Video = () => {
    return(
        <div className={s.video}>
            <p className={s.videoText}>Video</p>
        </div>
    )
};

export default Video;