import s from './settings.module.scss';

const Settings = () => {
    return(
        <div className={s.settings} >
            <p className={s.settingsText}>Settings</p>
        </div>
    )
};

export default Settings;