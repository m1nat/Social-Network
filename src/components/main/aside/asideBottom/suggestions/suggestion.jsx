import s from './suggestion.module.scss';

const Suggestions = () => {
    return(
        <div className={s.suggestions}>
            <p className={s.suggestionsTitle}>Suggestions:</p>
        </div>
    )
};

export default Suggestions;