import SuggestionStatusItem from "./suggestionStatusItem"

const mapStateToProps = state => {
  return {
    
  }
};

const mapDispatchToProps = dispatch => {
  return {

  }
}

const SuggestionStatusItemConstaner = connect()(SuggestionStatusItem)
export default SuggestionStatusItemConstaner